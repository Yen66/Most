# ADR-0006 — Supersede вместо потери истории

Версионируемые значения не редактируются в месте. Старая запись закрывается `valid_to`,
новая получает новый id, причина и актор фиксируются в журнале подтверждений.
Идентичный документ по SHA-256 повторно не импортируется.
