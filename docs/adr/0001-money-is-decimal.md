# ADR-0001 — Деньги только Decimal

Контекст: расчёты должны воспроизводиться до копейки.

Решение: `Decimal`, `ROUND_HALF_UP`; позиция округляется до 0,01, итог — сумма округлённых позиций.
При извлечении НДС корректируется НДС, а не net, чтобы `net + vat == gross`.
